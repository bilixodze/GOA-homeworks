# first task start

# Manual Sum Function: Create a function called manual_sum that iterates over list and adds their sum in a variable, then returns variable. Use for loop for this task

# def manual_sum(num_list, start_value = 0):
#     result = start_value

#     for num in num_list:
#         result += num
    
#     return result

# number_list = [1,2,3,4,5]

# print(manual_sum(number_list, 5))

# first task end



# second task start

# Manual Max Function: Define a function named manual_max that iterates through list, updating a variable with the maximum value, then returns the maximum value found. Use for loop for this task

# def manual_max(num_list):
#     max_value = num_list[0]

#     for num in num_list:
#         if max_value < num:
#             max_value = num

#     return max_value

# number_list = [1,2,3,22,4,55,12,13,4,123]

# print(manual_max(number_list))

# second task end



# third task start 

# Manual Min Function: Define a function named manual_min that iterates through list, updating a variable with the minimum value, then returns the minimum value found. Use for loop for this task

# def manual_min(num_list):
#     min_value = num_list[0]

#     for num in num_list:
#         if min_value > num:
#             min_value = num
    
#     return min_value

# number_list = [1,33,24,11,4,-23,12,17,-22]

# print(manual_min(number_list))

# third task end



# fourth task start

# Manual Len Function: Develop a function named manual_len that iterates through list, counting each element, and returns the count as the length of the list. Use for loop for this task

# def manual_len(collection):
#     count = 0

#     for _ in collection:
#         count += 1
    
#     return count 

# num_collection = [1,2,3,4]
# str_collectin = "onise"
# range_collection = range(10)

# print(manual_len(range_collection))
# print(manual_len(num_collection))
# print(manual_len(str_collectin))

# fourth task end  